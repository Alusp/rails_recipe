  <nav>
    <div class="">
      <div class="flex justify-between h-16 px-10 shadow items-center">
          <h1 class="text-xl lg:text-2xl font-bold cursor-pointer">RecipeApp</h1>
          <div class="hidden md:flex justify-around space-x-4 gap-12">
          <% if user_signed_in? %>
            <%= link_to "Public recipes", public_recipes_path, class: "hover:text-cyan-400 text-gray-700" %>
          <% end %>
          <% if current_user %>
            <%= link_to "Food", foods_path, class: "hover:text-cyan-400 text-gray-700" %>
            <%= link_to "Recipes", recipes_path, class: "hover:text-cyan-400 text-gray-700" %>
            <% end %>
          </div>
        <div class="flex space-x-4 items-center">
          <% if current_user %>
            <%= button_to "Sign out", destroy_user_session_path, method: :delete,  data: {turbo: false}, class: "border px-4 py-2 rounded text-black text-sm uppercase", 'data-ripple-light': true %>
          <% else %>
            <%= link_to "Log in", new_session_path(resource_name), class:"text-gray-800 text-sm uppercase" %> 
            <%= link_to "Sign up", new_registration_path(resource_name), class:"bg-cyan-400 px-4 py-2 rounded text-white hover:bg-cyan-400 uppercase text-sm" %>
          <% end %>

        </div>
      </div>
    </div>
  </nav>